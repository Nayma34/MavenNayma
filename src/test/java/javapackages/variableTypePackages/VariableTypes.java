package javapackages.variableTypePackages;

public class VariableTypes {
    //there are three types of variables:
    //i. instance/global variables-- the variables declares inside the class are called instance variable.
    //ii. local variables-- the variables declare inside the methods, are called local variables, local variable
    // born inside the method and die inside the method
    // iii. static variables-- static variables belongs to the class. this is called class variable. static variable
    //we create when we see the variable/variables are common to all objects


byte age= 100; // we declare the variable and initialize the variable/ define the variable
    short CheckingAccount; // we declare the variable


public void study (){ // function-- methods can hold variables, statements
    System.out.println(age);
    CheckingAccount= 32767; // initialize/ define the variable
    System.out.println(CheckingAccount);
    boolean snooringInTheClass;
    snooringInTheClass= true;
}



    public static void main(String[] args) {

        String name= "Nayma";
    }




















}
